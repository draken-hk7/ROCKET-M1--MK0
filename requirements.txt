matplotlib>=3.8
numpy>=1.26
pandas>=2.2
scipy>=1.12
pytest>=8.0
pytest-cov>=5.0
flake8>=7.0
mypy>=1.10
dash>=2.17
plotly>=5.22
pyserial>=3.5

# Optional local CAD package. Scripts fall back to deterministic STL/STEP
# manifests when CadQuery is not available in CI or restricted environments.
cadquery>=2.4; platform_system != "Linux"
