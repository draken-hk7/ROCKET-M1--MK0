# Core scripts are standard-library first so the safe educational workflow can
# run in restricted environments. These packages are useful for local extension.
matplotlib>=3.8
numpy>=1.26
pandas>=2.2
scipy>=1.12

# Optional heavyweight engineering packages intentionally not required by CI:
# cadquery>=2.4
# cantera>=3.0
# rocketpy>=1.0
# pyNastran>=1.4
